{
  "// note": "MCP servers for Roo Code / DevGPT. Roo reads .roo/mcp.json and uses the 'mcpServers' key. Same servers as .vscode/mcp.json — keep them in sync. Versions are illustrative; verify before running.",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github@2025.4.8"],
      "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "${env:GITHUB_TOKEN}" }
    }
  }
}
